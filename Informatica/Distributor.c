#include <stdio.h>
#include <stdlib.h>

int main()
{

    int M;

    printf("Ciao inserisici un euro: ");
    scanf("%d", &M);
    if (M >= 1)
    {
        printf("Ecco a te la );
    }
    else
    {
        printf("Non hai inserito un euro valido\n");
    }
    return 0;
}